export interface Movie {
  id: string;
  title: string;
  release_date: string;
}

export interface MovieDetails {
  id: string;
  title: string;
  release_date: string;
  director: string;
  producer: string;
}
